# Core AMG requirements
# No external dependencies for core library (intentionally minimal)

# HTTP API (Phase 5+)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
httpx>=0.24.0

# Framework Adapters (Optional)
langchain-core>=0.1.0 # For LangChain and Langflow support
langgraph>=0.0.1      # For LangGraph support
